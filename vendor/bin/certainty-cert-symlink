#!/usr/bin/php
<?php

/**
 * Proxy PHP file generated by Composer
 *
 * This file includes the referenced bin path (../paragonie/certainty/bin/certainty-cert-symlink) using eval to remove the shebang if present
 *
 * @generated
 */

$binPath = realpath(__DIR__ . "/" . '../paragonie/certainty/bin/certainty-cert-symlink');
$contents = file_get_contents($binPath);
$contents = preg_replace('{^#!/.+\r?\n<\?(php)?}', '', $contents, 1, $replaced);
if ($replaced) {
    $contents = strtr($contents, array(
        '__FILE__' => var_export($binPath, true),
        '__DIR__' => var_export(dirname($binPath), true),
    ));

    eval($contents);
    exit(0);
}
include $binPath;
